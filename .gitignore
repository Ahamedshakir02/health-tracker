node_modules
dist

# Local environment — the real Firebase config.
# Only .env.example and .env.offline belong in the repo; .env.offline holds
# nothing but blank keys, which is the point of it.
.env
.env.*
!.env.example
!.env.offline

# Build caches
*.tsbuildinfo
.vite
coverage

.DS_Store
Thumbs.db
*.log
